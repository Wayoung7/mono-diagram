binary_tree = { "binary_tree" }
table = { "table" }
grid = { "grid" }
dag = { "dag" }

title = { "[" ~ (binary_tree | table | grid | dag) ~ "]" }
content = { (!(title) ~ ANY)+ }

diagram = { title ~ content }
main = { SOI ~ (diagram*) ~ EOI }
